# Roadmap

- [x] Export full backend (tables, rules, stock triggers, live updates, shop data) as a one-click setup file for the user's own Supabase project
- [x] Write the builder prompt for the phone sales machine (mobile POS) app
- [ ] Optional: after the user runs the setup file, re-issue a device code and re-test a phone sale end to end
